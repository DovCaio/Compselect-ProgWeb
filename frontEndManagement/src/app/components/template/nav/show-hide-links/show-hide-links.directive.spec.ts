import { ShowHideLinksDirective } from './show-hide-links.directive';

describe('ShowHideLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideLinksDirective();
    expect(directive).toBeTruthy();
  });
});
